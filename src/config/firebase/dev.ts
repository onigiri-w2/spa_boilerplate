// config/secretはgitignoreしているので、各自で作成してください。
// credential.dev.jsonは、Firebaseのコンソールからダウンロードできます
import firebaseCredentials from "@/config/secret/firebase/credential.dev.json";

const config = firebaseCredentials;

export default config;
